import { DeploymentArgv } from '../types/deployment';

export const MTPPassport: DeploymentArgv = {
  name: 'Maju Tani Passport',
  symbol: 'MTPass',
  uri: 'ipfs://',
  deploymentPrefix: 'MTPPassport',
};